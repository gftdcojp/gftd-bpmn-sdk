import { describe, it, expect } from 'vitest';

// Test core exports
describe('@gftd/bpmn-sdk/core', () => {
  it('should export types', () => {
    // Test that core types are available
    expect(true).toBe(true);
  });

  it('should export IR types', () => {
    // Test that IR types are available
    expect(true).toBe(true);
  });
});

